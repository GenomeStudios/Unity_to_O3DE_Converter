"""Integration tests — full ``IntegratedAssetProcessor`` runs against
the synthetic Unity tree fixture. Exercise the F-9 emission path,
patch worker, orchestrator, and per-platform switching end-to-end."""
